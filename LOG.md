# LOG

Bitácora de cambios estructurales y de protocolo de este proyecto (no actividad normal de campaña, eso vive en campaign-map.md).

## 2026-06-14
- Setup: agregado LOG.md, AGENTS.md y handoffs/ para coordinación con Codex.

## 2026-06-25
- Infraestructura nueva: proyecto Supabase (`iwtzzusjuaymmixsvevx`, org zystfagnvpmlgfwcdmvk) conectado vía MCP (`claude mcp add --transport http supabase`). OAuth, sin PAT manual.
- Tabla `quiz_leads` (SQL en `supabase/001_quiz_leads.sql`): guarda cada respuesta del quiz de `califica.html` (contacto + banda + segmento + pago estimado + las 6 respuestas en `respuestas` jsonb). RLS activado, policy de solo INSERT para `anon`, sin SELECT/UPDATE/DELETE desde el cliente.
- `califica.html` ahora manda cada submit a 3 destinos en paralelo: Kit (captura de lead), Supabase (dato crudo), y dispara el correo de resultado (ver abajo). Mismo flujo, sin tocar Supabase, si se vuelve a cambiar el form de Kit.
- Kit: form dedicado para el quiz creado (`9611887`, "Captura2-calculadora-quiz"), separado del form de captura del modal landing (`9610315`, "Captura1-auto-opt"). Custom field nuevo: `rango_vehiculo` (no usado todavía en el insert, queda disponible).
- Envío del resultado por correo NO se hizo con Kit (las Visual Automations que conectan form -> secuencia son de pago). Se resolvió 100% gratis con Supabase + Resend:
  - Database Webhook en Supabase (`Database > Webhooks`, no vía API/MCP) dispara en cada INSERT a `quiz_leads`.
  - Edge Function `send-quiz-result` (Deno, desplegada vía MCP `deploy_edge_function`) recibe el webhook, recalcula banda/segmento/rango de vehículo en servidor (sin nuevas columnas) y manda el correo bilingüe (usa la columna `lang`) vía Resend API.
  - Secret `RESEND_API_KEY` vive en Supabase (`Project Settings > Edge Functions > Secrets`), nunca pasó por el chat.
  - Dominio `oscargetmeacar.website` (Namecheap) verificado en Resend (SPF + DKIM + DMARC, registros TXT/MX en sub-hosts `send` y `resend._domainkey`, sin necesidad de buzón de correo). Envía desde `resultados@oscargetmeacar.website`. Verificado end-to-end: insert -> webhook -> function (200) -> Resend -> Gmail real.
  - Gotcha: el dominio sandbox de Resend (`onboarding@resend.dev`) solo entrega al correo de la cuenta registrada, no a destinatarios reales. Hace falta dominio propio verificado para producción aunque sea plan gratis.
- UI: paso de opt-in (captura de email) ahora tiene efecto "glass" (`#optin.glass-step`, `backdrop-filter: blur`) flotando sobre el resultado ya pintado de fondo (`paintResult()` separado de `renderResult()` para pintar sin cambiar de pantalla).
- Favicon agregado (`favicon.ico` + pngs en `assets/`) generado con Python/Pillow centrando `logo-symbol-transparent.png` en lienzo cuadrado, sin recorte. Linkeado en `index.html` y `califica.html`.
- Flujo de git nuevo para este proyecto: rama `dev` para cambios antes de producción. Vercel ya tiene alias fijo por rama: `oscar-getmeacar-git-dev-agenc-ia1.vercel.app`, se actualiza solo con cada push a `dev`.
- Gotcha de sesión: las MCP tools de un servidor agregado a media sesión (`claude mcp add`) no aparecen hasta usar `ToolSearch` (o reiniciar Claude Code); el servidor puede mostrar "Connected" en `claude mcp list` sin que sus tools estén cargadas todavía.

## 2026-07-16
- Dashboard privado del quiz (cadena `marketing-dashboard-kit`) sobre el sitio estatico. Primeras funciones serverless del proyecto (`api/`), antes era 100% estatico.
  - SQL: funcion `dashboard_funnel_metrics()` (`SECURITY DEFINER`, `set search_path=public`, solo agregados de `quiz_leads`, cero filas crudas). Aplicada via MCP `apply_migration`, guardada en `supabase/002_dashboard_funnel_metrics.sql`.
  - `api/metrics.js`: capa de proyeccion. Funnel `live` via RPC con anon key; `emails` (Kit) y `bookings` (Cal.com) como adaptadores `pending` con `null` (nunca 0 falso). Verifica la cookie de sesion (401 sin ella).
  - `dashboard.html` (ruta `/dashboard`): hereda `colors_and_type.css`, copy en espanol, numeros en display, hairlines, dots live/pending. Dato faltante = en-dash (U+2013), NO em-dash (prohibido); la plantilla del skill usaba em-dash, se cambio.
  - Gate (`deploy-gated-site`): `middleware.js` (Edge, Web Crypto) protege `/dashboard`; `api/login.js` (Node crypto) valida SHA-256 del password y firma cookie HMAC `<exp>.<sig>`; `api/logout.js`; `login.html`. `.vercelignore` excluye `supabase/ handoffs/ *.md .env*` etc.
  - Env vars en Vercel (los 4 entornos): `DASHBOARD_PASSWORD_HASH`, `SESSION_SECRET`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`. El password (proyecto de clase) NO se pone aqui: el repo es publico y publicarlo anularia el gate; vive en las env vars de Vercel y en la memoria privada del agente.
  - Ruta canonica del panel: **`/dashboard.html`** (con `.html`). Este sitio NO tiene clean URLs: `/dashboard`, `/login`, `/califica` dan 404; solo `.html` resuelve. La plantilla del skill asumia clean URLs; se ajusto el gate para redirigir a `/login.html` y volver a `/dashboard.html`. Si algun dia se activa `cleanUrls: true` en `vercel.json`, revisar estas rutas.
  - Mergeado a `main` (fast-forward, commit `ab82258`) y **verificado en produccion publica** (`oscar-getmeacar.vercel.app`, sin SSO): `/dashboard.html` sin sesion 302 a `/login.html`, login 401/200, `/api/metrics` 200 con funnel live, archivo interno 404 por `.vercelignore`.
- Gotcha (confirmado): los preview por rama estan detras del **SSO de Deployment Protection de Vercel** (302 a `vercel.com/sso-api`), que intercepta ANTES de las funciones propias. Para smoke-test de un preview protegido: MCP `get_access_to_vercel_url` da un `?_vercel_share=<token>` que setea la cookie `_vercel_jwt`; guardarla en un cookie jar (`curl -c/-b`) y recien ahi se prueba el gate real. Produccion (`main`) es publica, sin este SSO.
- Gotcha: el conector MCP de Vercel puede desplegar (`deploy_to_vercel`) y leer, pero NO tiene tool para setear env vars; eso se hace en el dashboard web de Vercel o por CLI.

## 2026-06-26
- Primer lead magnet del proyecto: "El Checklist Antes de Firmar" (`checklist-financiamiento.html`), via el skill `/lead-magnet`.
- Decisión de placement: la idea original era convertir el error #3 de la sección Mistakes en el anuncio del lead magnet. Se descartó: convierte un insight genuino en autopromoción y choca con la voz de marca ("no promete lo imposible"). Se dejaron los 3 errores intactos y se agregó el CTA como bloque aparte, justo después de la lista, enganchado al error #3 (firmar sin entender el financiamiento). Lección para futuros lead magnets en esta landing: el magnet debe ofrecerse como solución a un dolor ya declarado, no reemplazar el dolor mismo.
- Kit: no hay tool MCP para crear un form/landing page nuevo, así que el opt-in del checklist reutiliza el form existente `9610315` (el del modal "Talk to Oscar"), diferenciando el lead con `fields[fuente]=lead_magnet_checklist`. Patrón a repetir: para nuevos magnets, reusar el form principal con un valor distinto en `fuente` en vez de pedir un form nuevo.
- Secuencia de 5 emails creada en Kit vía MCP (`create_sequence` + `create_sequence_email`, ID `2807117`) pero queda como **borrador sin publicar** a propósito: enviar emails reales a leads no es reversible, así que el flujo correcto es crear en borrador y que el humano revise antes de publicar.
- Gotcha importante: **las Visual Automations de Kit (form → secuencia automática) son feature de plan pago.** En plan gratis no hay forma de disparar la secuencia automáticamente cuando alguien se suscribe vía fetch. La entrega del lead magnet en sí no depende de esto (el navegador redirige directo al asset tras el submit), pero el seguimiento por correo sí queda manual/pendiente de upgrade. No asumir que toda automatización de Kit está disponible sin confirmar el plan primero.
- Gotcha de branches/Vercel: cada rama tiene su propio alias fijo (`dev` → `oscar-getmeacar-git-dev-agenc-ia1.vercel.app`), pero **production (`oscar-getmeacar.vercel.app`) solo se actualiza con merge a `main`.** Cambios que no aparecen en producción (ej. favicon) casi siempre es porque viven en `dev` sin mergear, no porque el archivo esté mal. Verificar primero en qué rama está cada URL antes de diagnosticar un bug.
- Gotcha de Vercel: las URLs de preview por rama pueden estar protegidas por SSO de Vercel (toda request, incluidos assets como el favicon, redirige a un login si no estás autenticado con la cuenta de Vercel del equipo).
- Bug detectado y corregido: el tagline del footer ("Honest options. Forward movement.") está hardcodeado en inglés sin `data-i18n` en `index.html` y se copió igual en `checklist-financiamiento.html`. Se corrigió en el checklist (ahora en español, esa página no tiene toggle de idioma). **Pendiente:** `index.html` sigue con el mismo tagline sin traducir aunque el resto de la página sí cambia de idioma (a diferencia de `califica.html`, que sí usa `data-i18n="foot_tagline"` correctamente).
- A diferencia del quiz (`califica.html`), el lead magnet del checklist **no guarda nada en Supabase**, solo en Kit. Si se quiere el dato crudo del lead también ahí, hay que replicar el patrón de `quiz_leads` (tabla + insert paralelo) para este flujo.
- Pase premium de diseño en `index.html` y `califica.html`: movimiento cinematográfico con GSAP (vía CDN, con guard `prefers-reduced-motion`), micro-interacción `.btn-reveal` en CTAs, y assets reales generados con Gemini Nano Banana Pro (ver memoria `image-generation-gemini`). Hero + 4 fotos de carro en `assets/photos/`.
- `califica.html`: flujo cambiado a **opt-in primero → resultado**. Se eliminó el patrón "glass tease" (`#optin.glass-step`) porque el resultado nuevo (header navy + foto de carro + count-up) es muy alto y dejaba ver el resultado antes del correo. Resultado premium con foto de carro por segmento, números con count-up y saludo personalizado con el nombre.
- Cal.com integrado: evento `oscargmc/consulta` (citas de 1h). En el resultado, el botón "Agenda tu cita con Oscar" abre el popup embed (temado navy, responsive) con prefill de nombre/correo/notas (banda + segmento + pago). Reemplazó el CTA viejo que reabría el formulario del modal del landing (re-pedía datos ya capturados).
- Email de resultado (Edge Function `send-quiz-result`, **v3**) rediseñado a nivel marca: header navy con badge de banda, pago mensual protagonista, botón "Agenda tu cita" con Cal.com prefijado, y foto del carro por segmento (usa URLs de prod `oscar-getmeacar.vercel.app/assets/photos/`). Sigue bilingüe + Resend. La foto solo carga bien una vez los assets están en prod público.
- **FUENTES (importante):** `colors_and_type.css` usa las Söhne de PRUEBA (`TestSohne-*`, distribución de evaluación de Klim Type Foundry), NO licenciadas para uso comercial en producción. Por ahora se deja así porque es un prod de prueba (no se vende nada). **Próxima sesión: evaluar cambiar a Inter (ya incluida, gratis) como alternativa, o licenciar Söhne en klim.co.nz antes del lanzamiento real.**
- Legales: `privacidad.html` + `cookies.html` creados con placeholders (`[NOMBRE LEGAL DEL NEGOCIO]`, `[CORREO DE CONTACTO]`, `[DIRECCIÓN / CIUDAD]`, `[TELÉFONO]`). Por decisión (Opción B) quedan **desenlazados del footer** para no exponer placeholders en prod; las páginas se despliegan pero solo son accesibles por URL directa. Rellenar los 4 datos y re-enlazar antes del lanzamiento real.
- Publicado el pase completo a `dev` y `main` (commit `7396def`, fast-forward `dev:main`). Prod live y verificado: https://oscar-getmeacar.vercel.app (landing + `califica.html` + assets públicos). Nota: `.claude/settings.local.json` se dejó fuera del commit a propósito (config local).
- `quiz_leads`: limpiados 7 leads de prueba (alias `jesuspedroza+...` y el `fuente='quiz_test_email'`) tras verificar el correo end-to-end. Para test del correo: insertar fila en `quiz_leads` dispara el webhook -> Edge Function -> Resend; recordar limpiar después.
- Bug de móvil reportado (layout "se sale" en el landing). Una sesión de Claude Code desde el celular sí lo trabajó pero el fix quedó colgado en la rama `claude/project-visual-details-tgaay6` (commit `aa23b92`) **sin mergear a `main`**, por eso seguía en prod. Cherry-pick de ese commit a `dev` (recorte de la foto de Oscar + `.lm-form` con `min-width:0` para que el input del checklist no desborde) + arreglo adicional del header en `@media (max-width:520px)` (el header `position:fixed` con logo + toggle + CTA `nowrap` se iba a ~390px en teléfonos de 360-390px). Gotcha: las sesiones desde la app del celular pushean a una rama `claude/...` propia, NO a `dev`/`main`; revisar `git fetch --all` y mergear esas ramas a mano o el cambio nunca llega a producción.
